<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <package android:name="com.example.marsphotos" />
<!--        aidl -3-->
    </queries>

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Boschcompose1"
        tools:targetApi="31">
        <receiver
            android:name="com.example.marsphotos.SmsReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="5">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
                <category android:name="android.intent.category.DEFAULT"/>

            </intent-filter>
        </receiver>

        <activity
            android:name="com.example.marsphotos.BottomNavActivity"
            android:exported="true"
            android:label="@string/title_activity_bottom_nav"></activity>
        <activity
            android:name="com.example.marsphotos.ContactActivity"
            android:exported="false" />
        <activity
            android:name="com.example.marsphotos.DiceRollerActivity"
            android:exported="true"
            android:label="@string/title_activity_dice_roller"
            android:theme="@style/Theme.Boschcompose1"></activity>
        <activity
            android:name="com.example.marsphotos.XmlActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.marsphotos.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Boschcompose1" />
    </application>

</manifest>